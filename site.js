function openLinkedDetails(){ const id=location.hash.slice(1); const target=document.getElementById(id); if(target&&target.tagName==='DETAILS')target.open=true; } window.addEventListener('hashchange',openLinkedDetails); document.querySelectorAll('a[href="#research-story"]').forEach(a=>a.addEventListener('click',()=>{document.getElementById('research-story').open=true;})); openLinkedDetails();
